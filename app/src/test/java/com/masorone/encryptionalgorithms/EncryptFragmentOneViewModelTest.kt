package com.masorone.encryptionalgorithms

import kotlinx.coroutines.flow.MutableStateFlow
import org.junit.Assert.*
import org.junit.Test

class EncryptFragmentOneViewModelTest {

    @Test
    fun `test both empty`() {

//        val repository = MessageRepository.Base(mutableListOf())
//        val communication =
//        val viewModel = EncryptFragmentOneViewModel(repository)
//        viewModel.send(message = "", key ="")
    }
}